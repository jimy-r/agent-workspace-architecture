# Stub project CLAUDE.md (Canary C2)

<-- Describe what this workspace is for -->

**Audit canary fixture (R4). Do not action.** This file deliberately contains
the boilerplate placeholder pattern `<-- Describe what this workspace is for -->`
so the audit's Phase 2 CLAUDE.md-quality check keeps catching stub files.
If the audit stops reporting `CANARY-CONFIRMED: C2`, the stub-detection
check has regressed.

See `tests/audit_canaries/canary.json` for the full manifest.
